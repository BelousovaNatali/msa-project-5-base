import { Schema, ShapeSerializer } from "@smithy/types";
import { SerdeContextConfig } from "@aws-sdk/core/dist-types/ts3.4/submodules/protocols/ConfigurableSerdeContext";
import { JsonSettings } from "@aws-sdk/core/dist-types/ts3.4/submodules/protocols/json/JsonCodec";
export declare class SinglePassJsonShapeSerializer
  extends SerdeContextConfig
  implements ShapeSerializer<string>
{
  readonly settings: JsonSettings;
  private buffer;
  private rootSchema;
  constructor(settings: JsonSettings);
  write(schema: Schema, value: unknown): void;
  writeDiscriminatedDocument(schema: Schema, value: unknown): void;
  flush(): string;
  private writeObject;
  private writeValue;
}
