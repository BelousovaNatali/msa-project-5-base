import { isReadableStream } from "@smithy/core/dist-es/submodules/serde/util-stream/stream-type-check";
import { ChecksumStream } from "@smithy/core/dist-es/submodules/serde/util-stream/checksum/ChecksumStream";
import { createChecksumStream as createChecksumStreamWeb, } from "@smithy/core/dist-es/submodules/serde/util-stream/checksum/createChecksumStream.browser";
export function createChecksumStream(init) {
    if (typeof ReadableStream === "function" && isReadableStream(init.source)) {
        return createChecksumStreamWeb(init);
    }
    return new ChecksumStream(init);
}
