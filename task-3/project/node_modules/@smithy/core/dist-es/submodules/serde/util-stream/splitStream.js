import { PassThrough } from "node:stream";
import { splitStream as splitWebStream } from "@smithy/core/dist-es/submodules/serde/util-stream/splitStream.browser";
import { isBlob, isReadableStream } from "@smithy/core/dist-es/submodules/serde/util-stream/stream-type-check";
export async function splitStream(stream) {
    if (isReadableStream(stream) || isBlob(stream)) {
        return splitWebStream(stream);
    }
    const stream1 = new PassThrough();
    const stream2 = new PassThrough();
    stream.pipe(stream1);
    stream.pipe(stream2);
    return [stream1, stream2];
}
